import { createTheme } from "@mui/material";

createTheme;

export const customTheme = createTheme({
  palette: {
    primary: {
      main: "#1e1e1e",
      secondary: "#ffb703",
    },
    secondary: {
      main: "#ffb703",
      secondary: "#023047",
    },
  },
});
